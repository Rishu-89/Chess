<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ChessMaster Arena</title>
    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #1c2833;
            color: #ecf0f1;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
        }

        .navbar {
            width: 100%;
            background-color: #34495e;
            padding: 15px 20px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
            display: flex;
            justify-content: center;
        }

        .navbar .brand {
            color: #ecf0f1;
            font-size: 28px;
            font-weight: bold;
            text-transform: uppercase;
            letter-spacing: 2px;
        }

        .content {
            text-align: center;
            margin-top: 50px;
            max-width: 500px;
            padding: 20px;
            background-color: #2c3e50;
            border-radius: 10px;
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.3);
        }

        .content h1 {
            font-size: 22px;
            margin-bottom: 20px;
        }

        .content h3 {
            margin: 20px 0;
            font-size: 18px;
        }

        .start-button {
            background-color: #e74c3c;
            color: white;
            border: none;
            padding: 15px 40px;
            font-size: 20px;
            cursor: pointer;
            transition: background-color 0.3s ease;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
        }

        .start-button:hover {
            background-color: #c0392b;
        }

        .copy-button {
            background-color: #3498db;
            color: white;
            border: none;
            padding: 10px 20px;
            font-size: 16px;
            cursor: pointer;
            transition: background-color 0.3s ease;
            border-radius: 5px;
            margin-top: 15px;
        }

        .copy-button:hover {
            background-color: #2980b9;
        }

        .link-container {
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        .link-container a {
            color: #3498db;
            text-decoration: none;
            font-size: 18px;
        }
    </style>
</head>
<body>
    <div class="navbar">
        <div class="brand">ChessMaster Arena</div>
    </div>
    <div class="content">
        <h1>Share This Link to Start the Game:</h1>
        <div class="link-container">
            <a id="gameLink" href="#"></a>
            <button class="copy-button" onclick="copyLink()">Copy Link</button>
        </div>
        <h3>OR</h3>
        <form action="/start" method="post">
            <button class="start-button">Start Game</button>
        </form>
    </div>

    <script src="javascript/start.js"></script>
    
</html>

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Chess Game</title>
    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: Arial, sans-serif;
            background-color: #1d1f21;
            color: #ecf0f1;
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        .navbar {
            width: 100%;
            background-color: #2c3e50;
            padding: 15px;
            box-shadow: 0 4px 2px -2px gray;
        }

        .navbar .brand {
            color: #ecf0f1;
            font-size: 24px;
            font-weight: bold;
            text-align: center;
        }

        .content {
            display: flex;
            flex-direction: column;
            align-items: center;
            margin-top: 50px;
        }

        .start-button {
            background-color: #e74c3c;
            color: white;
            border: none;
            padding: 15px 30px;
            font-size: 18px;
            cursor: pointer;
            transition: background-color 0.3s ease;
            border-radius: 5px;
        }

        .start-button:hover {
            background-color: #c0392b;
        }
    </style>
</head>
<body>
    <div class="navbar">
        <div class="brand">Lichess Clone</div>
    </div>
    <div class="content">
        <button class="start-button" onclick="generateChessboard()">Start Game</button>
    </div>

   
</body>
</html>
